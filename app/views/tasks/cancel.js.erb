//-----------------------------------------------------------------------------
// app/views/tasks/cancel.js.erb
//-----------------------------------------------------------------------------

// Remove the model form, so there are not duplicates
$("div#task-modal-body").find("#edit-task-form").remove();

// Hide modal add task window if displayed
$('#addTaskModal').modal("hide");
